# 검색 아키텍처 결정 기록

> 이 기록은 이 도구가 만들어진 원 프로젝트(한국어+영문 혼합 개발 문서 코퍼스)의 결정을
> 일반화해 옮긴 것이다. 코드 주석의 `R-001` 참조는 이 문서를 가리킨다.

---

## R-001 · 검색은 BM25 단독으로 간다 (v1)

- **결정일** 2026-08-13
- **상태** 채택
- **범위** `indexer.py`, `search.py`, `tokenizer.py`

### 결정

v1 은 **BM25 단독**. 임베딩·벡터DB를 쓰지 않는다.
외부 API 키가 필요 없고, 문서가 외부로 나가지 않으며, 전부 로컬에서 계산된다.

### 근거

**원 코퍼스 실측 (2026-08-13, 개발 문서 203개 / 약 400만 자)**

| 관측 | 수치 | 함의 |
|---|---|---|
| 개발용 문서의 한글 비중 | 17.7~20.9% (API·DB·알고리즘 문서) | 임베딩의 의미 이점이 작동할 표면적이 좁다 |
| 영문·숫자 비중 | 38~44% | 검색의 주 신호(엔드포인트·필드명·ID)가 이미 렉시컬 |
| 코드블록 / 표 행 | 54.8% / 53.8% (겹침 있음) | 표·코드는 임베딩이 뭉갠다 |
| 식별자 밀도 | `D-52`, `API-084`, `FN-041` 류 다수 | **임베딩은 `D-52`와 `D-53`을 구분하지 못한다** |
| 버전 접미사 파일 | 102 / 203 | 문서가 자주 갱신됨 → 재색인 비용이 반복됨 |

> 자기 코퍼스가 이 프로파일과 다르면(예: 순수 산문 위주) 결론도 달라질 수 있다 —
> 같은 실측(한글 비중·식별자 밀도·코드/표 비중)을 먼저 해 보기를 권한다.

**연구**

- [BM25 Wins at Scale](https://arxiv.org/html/2607.26497v2) — Recall@20 을 제외한 모든 지표에서 BM25 > `text-embedding-3-large`
- [BRIGHT / 에이전틱 검색](https://rajivshah.com/blog/rag-agentic-world.html) — **BM25 + 에이전틱 루프가 고급 임베딩 단독을 능가**. 이 도구의 구조(서브에이전트가 반복 질의)가 정확히 이것
- 다만 모든 연구가 **하이브리드를 최소 기준선으로 권고**한다 → 최종형은 하이브리드. 지금은 순서의 문제

**운영**

- 팀원 수만큼 API 키·비용·설정이 늘지 않는다
- 법무·사업 문서를 포함한 코퍼스 전체가 외부로 나가지 않는다
- 검색 결과를 **디버깅할 수 있다** — 어떤 토큰이 매칭됐는지 그대로 보인다. 벡터는 "모델이 그렇게 판단했다"로 끝난다

### 검토했으나 채택하지 않은 것

| 안 | 기각 사유 |
|---|---|
| 벡터 단독 (외부 임베딩 API) | 식별자 검색 불가, 표·코드 취약, 외부 전송, 재색인 비용 |
| 하이브리드 즉시 도입 | 측정 없이 도입하면 **벡터가 실제로 몇 %를 건졌는지 영영 모른다**. 비용만 2배 |
| SQLite FTS5 | 기본 토크나이저가 CJK 를 토크나이즈하지 않고, `trigram` 은 3글자 미만을 색인 못 함 → "코스"·"결정"·"인증" 같은 2글자 한국어가 통째로 누락 |
| 개발 문서 영어 번역본 유지 | 개발 문서는 이미 한글이 18~23%뿐이라 이득이 작고, **정본이 둘이 되어 stale 관리가 2배**가 된다 |
| LLM 기반 검색 | "LLM 으로 의미 검색 보완"은 **호출하는 코딩 에이전트(서브에이전트)가 이미 하는 일** — 에이전트를 하나 더 붙이는 중복 |

### 남겨둔 이음매 (하이브리드 대비)

1. **`search.py` 인터페이스 고정** — 반환 타입(`SearchResult` / `Hit`)을 검색 방식과 무관하게 유지한다.
   벡터를 붙일 때 `search()` 내부만 바뀌고, `mcp_server.py` · `server.py` · 호출하는 에이전트는 그대로다.
2. **점수 정규화 자리 확보** — BM25 점수를 그대로 노출하지 않고 0~1 로 정규화해서 낸다.
   나중에 RRF(Reciprocal Rank Fusion)로 합칠 때 그대로 쓰인다.
3. **검색 로그** — 질의 · 반환 조각 · 카테고리 · 결과 수를 남긴다(`logs/queries.jsonl`).
   아래 전환 판단의 유일한 근거다. `log_reader.py` 가 판독한다.
4. **청크 단위 유지** — 벡터를 붙여도 같은 청크를 임베딩하면 된다. 재청킹이 필요 없다.

### 하이브리드로 전환할 조건 (측정 기준)

아래 중 **하나라도** 관측되면 R-002 를 열고 재검토한다.

- 검색 로그에서 **"문서에 있는데 용어가 달라 못 찾음"이 20% 초과**
- 호출 에이전트의 **재질의(같은 의도로 2회 이상 검색) 비율이 30% 초과**
- 도메인 용어를 모르는 신규 인원 유입이 늘어, 용어집(`glossary.tsv`) 유지로 감당이 안 될 때

측정 없이 "정확도가 아쉽다"는 체감만으로 전환하지 않는다.

참고 — **임베딩은 반환 토큰량을 줄여주지 않는다.** 하이브리드도 같은 청크를 반환하므로
컨텍스트 절감 효과는 BM25 단독과 동일하다. 임베딩이 사는 것은 용어 불일치 질의의
재현율(recall)이고, 그 값어치는 위 조건이 실측으로 발화했을 때만 성립한다.

### 전환 시 해야 할 일 (예상)

| 순서 | 작업 | 비고 |
|---|---|---|
| 1 | 임베딩 제공자 결정 | 외부 API vs 로컬(sentence-transformers). **로컬이면 외부 전송 0 유지** |
| 2 | `vector_index.py` 추가 | 같은 청크를 임베딩. `search.py` 는 건드리지 않음 |
| 3 | `search.py` 내부에 RRF 병합 | BM25 순위 + 벡터 순위를 융합 |
| 4 | A/B 측정 | 전환 조건에서 쓴 로그를 그대로 평가셋으로 사용 |

### 지금 하지 않지만 예정된 보완 (하이브리드와 무관)

- **용어집 쿼리 확장** (`glossary.tsv`) — 동의어 문제를 임베딩 없이 상당 부분 흡수한다. v1 포함
- **폐기/stale 문서 강등** — 머리말 마커를 status 메타데이터로 파싱해 점수를 낮춘다. v1 포함
- **계약 문서 구조화** — API 명세 → OpenAPI, DB 설계 → 스키마. 번역보다 우월하나 별도 과제
